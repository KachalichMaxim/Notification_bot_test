# Как найти свой Bitrix24 User ID

## Способ 1: Через URL профиля (Самый простой) ⭐

1. Откройте Bitrix24: https://intranet.vedagent.ru
2. Войдите в свой аккаунт
3. Нажмите на свой **аватар/имя** в правом верхнем углу
4. Выберите **"Мой профиль"** или **"Профиль"**
5. Посмотрите на **URL в адресной строке браузера**

   Должно быть что-то вроде:
   ```
   https://intranet.vedagent.ru/company/personal/user/123/
   ```
   
   **Число в конце (123) - это ваш Bitrix24 User ID**

## Способ 2: Через создание задачи

1. Создайте тестовую задачу в Bitrix24
2. Если у вас включен DEBUG режим в webhook, посмотрите логи:
   ```bash
   ssh bookntrack@89.169.173.221
   cd ~/Notification_bot_test
   tail -f app.log
   ```
3. В данных задачи будет поле `CREATED_BY` с вашим ID

## Способ 3: Через админ-панель

1. Зайдите в **Настройки** → **Пользователи**
2. Найдите себя в списке пользователей
3. Откройте свой профиль
4. ID будет в URL страницы профиля

## Способ 4: Через REST API (если есть токен)

Если у вас есть токен доступа к REST API:

```bash
curl "https://intranet.vedagent.ru/rest/user.current.json?auth=YOUR_AUTH_TOKEN"
```

В ответе будет поле `ID` с вашим User ID.

## После того как найдете ID

### 1. Добавьте себя как руководителя:

```bash
ssh bookntrack@89.169.173.221
cd ~/Notification_bot_test
source venv/bin/activate
python manage_mappings.py add-leader <ВАШ_BITRIX_ID>
```

### 2. Добавьте Telegram маппинг:

```bash
python manage_mappings.py add-telegram <ВАШ_BITRIX_ID> 79865798
```

### 3. Проверьте настройки:

```bash
python manage_mappings.py list
python manage_mappings.py check-leader <ВАШ_BITRIX_ID>
python manage_mappings.py get-chat <ВАШ_BITRIX_ID>
```

## Пример

Если ваш Bitrix24 User ID = `456`, то команды будут:

```bash
python manage_mappings.py add-leader 456
python manage_mappings.py add-telegram 456 79865798
python manage_mappings.py list
```

Результат:
```
📋 Current Mappings:
Leaders: ['456']
Telegram Chats: {'456': '79865798'}
```

## Важно

- **Bitrix24 User ID** - это число (например: 123, 456, 789)
- **Telegram Chat ID** - это число (например: 79865798)
- Оба ID должны быть строками в JSON, но можно указывать как числа при добавлении

