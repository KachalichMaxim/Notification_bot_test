# Быстрая активация подписки Bitrix24

## ⚠️ Важно: Типы webhook токенов

В Bitrix24 есть **два типа** webhook токенов:

1. **Входящий webhook (Incoming)** - для отправки данных В Bitrix24
   - Используется для вызова REST API методов
   - Формат: `https://portal.bitrix24.ru/rest/USER_ID/TOKEN/`

2. **Исходящий webhook (Outgoing)** - для получения данных ИЗ Bitrix24
   - Используется для подписки на события
   - Формат: `https://portal.bitrix24.ru/rest/event.bind.json?auth=TOKEN`

**Для подписки на события нужен ИСХОДЯЩИЙ webhook!**

---

## 🚀 Способ 1: Через веб-интерфейс Bitrix24 (Самый простой)

### Шаг 1: Создайте исходящий webhook

1. Войдите в Bitrix24: https://intranet.vedagent.ru
2. Перейдите: **Настройки** → **Настройки портала** → **Вебхуки**
3. Найдите раздел **"Исходящие вебхуки"** или **"Outgoing Webhooks"**
4. Нажмите **"Создать"** или **"Добавить"**

### Шаг 2: Настройте webhook

- **URL обработчика:** `http://bookntrack.online:8081/webhook_tasks`
- **События:**
  - ✅ `OnTaskAdd` (создание задачи)
  - ✅ `OnTaskUpdate` (обновление задачи)
- **Сохраните**

### Шаг 3: Проверьте работу

1. Создайте тестовую задачу в Bitrix24:
   - Статус: **Важно**
   - Приоритет: **Высокий** (2) или **Критический** (3)
   - Создатель: Вы (ID: 488)

2. Проверьте логи:
```bash
ssh bookntrack@89.169.173.221 "tail -f ~/Notification_bot_test/access.log"
```

Если в логах появился POST запрос - всё работает! ✅

---

## 🔧 Способ 2: Через REST API (Если веб-интерфейс недоступен)

### Шаг 1: Получите исходящий webhook токен

Если у вас уже есть исходящий webhook, найдите его токен в настройках Bitrix24.

Если нет - создайте через веб-интерфейс (см. Способ 1).

### Шаг 2: Обновите .env файл

```bash
# На сервере
cd ~/Notification_bot_test
nano .env
```

Замените `BITRIX24_AUTH_TOKEN` на исходящий webhook токен.

### Шаг 3: Запустите скрипт подписки

```bash
source venv/bin/activate
python subscribe_bitrix24.py
```

---

## 🔍 Как проверить, что подписка активна

### Вариант 1: Через REST API

```bash
curl "https://intranet.vedagent.ru/rest/event.get.json?auth=ВАШ_ИСХОДЯЩИЙ_ТОКЕН"
```

Вы должны увидеть список активных подписок, включая вашу.

### Вариант 2: Тестовая задача

1. Создайте задачу в Bitrix24
2. Проверьте логи webhook сервиса
3. Если появился POST запрос - подписка работает!

---

## ❌ Устранение проблем

### Ошибка 401: Unauthorized

**Причина:** Используется входящий webhook вместо исходящего.

**Решение:** Создайте исходящий webhook в Bitrix24 и используйте его токен.

### Подписка создана, но события не приходят

**Проверьте:**
1. URL доступен из интернета: `curl http://bookntrack.online:8081/health`
2. Порт 8081 открыт в firewall (уже сделано ✅)
3. Подписка активна: `curl "https://intranet.vedagent.ru/rest/event.get.json?auth=ТОКЕН"`

### Где найти исходящий webhook в Bitrix24?

Путь может отличаться в зависимости от версии:
- **Настройки** → **Настройки портала** → **Вебхуки** → **Исходящие**
- **Приложения** → **Вебхуки** → **Исходящие**
- **Интеграции** → **Вебхуки** → **Исходящие**

Если не можете найти - обратитесь к администратору Bitrix24.

---

## 📝 Краткая инструкция

1. **Создайте исходящий webhook в Bitrix24:**
   - URL: `http://bookntrack.online:8081/webhook_tasks`
   - События: `OnTaskAdd`, `OnTaskUpdate`

2. **Проверьте работу:**
   - Создайте тестовую задачу
   - Проверьте логи: `tail -f ~/Notification_bot_test/access.log`

3. **Готово!** ✅

---

## 💡 Примечание

Если Bitrix24 создал подписку автоматически при создании исходящего webhook, дополнительная активация через скрипт не требуется. Просто создайте тестовую задачу и проверьте логи!

